name: run-test-suite

run-name: run test suite

on: [push]

jobs:
  Run-Test-Suite:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - run: echo "done {{ $default-branch }}"

